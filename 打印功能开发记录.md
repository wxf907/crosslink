# CrossLink 打印功能开发记录（2.3.3 → 2.4.0）

> 本记录由 2026-09-09 ~ 09-10 在第二台开发机上完成的工作整理而成，
> 供合并时对差异、沉淀技能库使用。所有结论均经真机验证。

## 一、路线决策

**放弃"自建 IPP 服务器为主"路线，转 Windows 原生 SMB 打印共享。**
2.3.3 的提交信息已记录原因：Win10 19044 的 IPP 客户端（CIM Add-Printer /
向导）对自建 IPP 服务器支持残缺，实测两种连接方式均失败。IPP 服务保留
（供手机/未来设备），UI 降级为"高级"折叠项。

实际部署形态（已验证）：
- 打印机 USB 接主机（Canon MG3600，Microsoft IPP Class Driver）
- 主机端一键共享为 `CrossLinkPrint`（SMB）
- 同事端粘贴连接串一键安装，之后任何软件 Ctrl+P 直接打印
- 第二台电脑（管理员模式）端到端实测通过：cmdkey + printui 安装 + 打印成功

## 二、主机端"一键启用"从未成功的四个根因（print_share_service.dart）

全部实锤并修复，任何一条都能让启用流程死在半路：

1. **.ps1 脚本无 BOM**：PowerShell 5.1 把无 BOM 脚本按 ANSI/GBK 解析，
   脚本里的中文注释直接 ParseError，提权进程一步没执行。
   修复：`writeAsString('\uFEFF$script')`，BOM 必须在文件首字节。
2. **防火墙组名**：`netsh ... group="File and Printer Sharing"` 在中文
   系统匹配不到任何规则（组名本地化）。修复：PS 原生
   `Get-NetFirewallRule -Group "@FirewallAPI.dll,-28502" | Set-NetFirewallRule`。
3. **printui /Xs 共享打印机 exit=1 不可靠**：修复改用
   `Set-Printer -Shared $true -ShareName`。
4. **结果日志编码**：脚本 `Set-Content` 默认 GBK，Dart `readAsString` 按
   UTF-8 解码抛异常被 catch 吞掉 → 配置明明成功了 App 永远显示失败。
   修复：所有日志写入加 `-Encoding UTF8`。

另：`secedit /configure` 即使成功也返回 exit=1（退出码不可靠，实测重新
导出策略确认已生效），判断以导出内容为准，勿信退出码。

## 三、同事端（installFromSpec）

- PrintNightmare 补丁后普通权限装驱动被拒（0x0000011b / 0x00000709），
  已加自动修复：检测到这两个错误码 → 提权改
  `RestrictDriverInstallationToAdministrators=0` → 自动重试。
- 添加打印机保留双重路径：WScript.Network COM → 失败回退 printui /in。
- 凭据写入用 cmdkey，账户名必须带主机前缀（`IP\clprint`）避免域解析歧义。

## 四、停止共享停错对象 bug（重要）

现象：停止共享后打印机仍在共享、首页灯仍绿，像"自动共享了"。
根因：`disableShare` 按"用户所选打印机"停止，但所选（G3020）与实际共享的
（MG3600）可能不一致 → 停了个未共享的对象，静默成功。
修复：
- `disableShare()` 不再收打印机参数，脚本内查所有挂 `CrossLinkPrint`
  共享名的打印机逐一取消；
- 新增 `sharedPrinter()` 返回实际共享的打印机名（PowerShell 输出需设
  `[Console]::OutputEncoding=UTF8` + Dart `stdoutEncoding: utf8`，否则
  中文打印机名乱码）；
- UI 记录 `_sharedName`，确认弹窗写明停哪台；共享名与所选不一致时主卡
  出橙色提示。

## 五、打印机在线探测（printer_probe.dart，新文件）

背景：spooler 对关机网络打印机报缓存"就绪"，此前的灰灯是错误绑定 IPP
开关的巧合，真实在线检测从未存在过；而驱动级能力查询
（DeviceCapabilitiesW）对离线网络打印机会触发"等待连接"弹窗并阻塞。

方案（全异步、不碰驱动、零弹窗）：
- IP 端口（IP_x.x.x.x）→ TCP 直连 9100/631，400ms 超时
- WSD 端口（WSD-<uuid>）→ WS-Discovery 组播探测（239.255.255.250:3702），
  按端口名 UUID 匹配应答，900ms 窗口
- USB 等本地端口 → 返回 null 交给 spooler 状态位

页面 10 秒轻量轮询（仅状态位+探测）；能力查询只在进页/切换打印机且
就绪时做（能力不变，且原生侧 IsPrinterOffline 预检已挡住离线场景）。
注意：文件互传的设备发现（UDP 广播）是独立模块，与打印轮询互不影响。

## 六、单实例僵尸进程 bug（main.dart，重大）

根因：第二实例检测到已存在实例后调 Dart `exit(0)`，在 Flutter 引擎启动
早期执行会触发 DLL 卸载死锁 → 进程变不可杀僵尸（taskkill/Stop-Process
全无效，1 线程）→ 僵尸持有 `CrossLink_SingleInstance_Mutex` 永不释放 →
此后所有启动连锁变僵尸，exe 被锁导致安装器 exit 5。
修复：改 `TerminateProcess(GetCurrentProcess(), 0)` 硬终止（不跑析构与
DLL 卸载，物理上无法卡死）+ FindWindow 增加窗口类
`FLUTTER_RUNNER_WIN32_WINDOW` 兜底（僵尸/早期实例标题未设置）。
遗留：第一实例自身退出（托盘退出等）理论上仍可能偶发同款死锁，未根治；
再遇杀不掉的进程重启即可，后续可考虑正常退出也走 TerminateProcess。

## 七、UI 重构（print_service_page.dart）

- 按角色分流：顶部「共享我的打印机 / 连接同事的」二选一，每屏一个主卡
- IPP 折叠进"高级"ExpansionTile，默认收起
- 主卡三态：未共享（醒目"启用共享"）/ 已共享有串（展示+复制）/ 已共享
  无串（明确"共享已在生效"，只留低调"重新生成"，防新手误点致旧串失效）
- 首页打印灯由 `AppState.smbShared` 驱动（原错绑 IPP 开关恒灰），
  `AppState.init` 启动即探测 isShared()，不依赖进过打印页
- 打印机能力标签缩为状态行灰字后缀"支持双面/彩色（打印方在打印对话框里选）"

## 八、安装包（installer/crosslink_setup.iss）

- 三处路径已改为本机 `D:\Workspace\crosslink_src`（合并后需改回）
- 新增 [Code]：`PrepareToInstall` 与卸载前主动 taskkill（先温和后 /F），
  解决应用占用文件导致安装中止（exit 5）。注意 [Code] 段注释必须用 `//`
- Inno Setup 7.1.0 编译通过；构建机装法（免提权）：
  `innosetup-7.1.0-x64.exe /VERYSILENT /CURRENTUSER /DIR=D:\dev\tools\InnoSetup`

## 九、构建环境备忘（第二台开发机）

- VS Build Tools 2026（C:\Program Files (x86)\Microsoft Visual Studio\18）
  需补 ATL 组件（screen_capturer 插件要 atlimage.h）：
  `setup.exe modify --add Microsoft.VisualStudio.Component.VC.ATL`
- GitHub 直连被墙，下载走 ghproxy.net 镜像 + `curl -C -` 断点续传
- 测试：`flutter analyze` + `flutter test` 全绿；
  live 真机测试 `test/live_share_test.dart` 需 `LIVE=1`（会弹 UAC）

## 十、待办（移交）

- [ ] README"共享打印（IPP）"段落已过时，改为 SMB 为主的描述
- [ ] pubspec.yaml 中文注释在第二台机器上显示乱码（不影响构建），建议修复
- [ ] 第一实例退出路径的 DLL 卸载死锁未根治（见第六节）
- [ ] tools/release.ps1 里 flutter/git 路径是第二台机器的，合并后改回
- [ ] 本机实际部署状态：MG3600 已共享为 CrossLinkPrint，clprint 账户密码
      为 ClLive2026Test（第二台电脑凭据与之配套；正式发版前建议换掉）
